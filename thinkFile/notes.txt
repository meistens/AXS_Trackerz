Server -> https://deep-index.moralis.io/api/v2.2
- Get NFT by wallet address
    /{address}/nft

    **
    /walletAddr/
    nft
    ?chain=ronin --> (can be an array)
    &format=decimal --> (always decimal, see no need for hex)
    &limit=100 --> (default can be preset lower)
    &exclude_spam=true --> (always true)
    &normalizeMetadata=true --> (always show, see what happens when false)
    &media_items=true --> (since cli, default will be false)
    &include_prices=true --> (always true)
    **

    What's Needed
    {
        amount
        token_id
        token_address
        contract_type
        owner_of
        name
        normalizedMetadata.name
        normalizedMetadata.description
        normalizedMetadata.attributes
        list_price.price
        list_price.currency
        list_price.price_usd
        list_price.marketplace
        floor_price_usd
        symbol
        minter_address
        possible_spam
        last_sale

        as experimentation...
       ** normalizeMetadata.attributes.trait_type|value (which will it show..)
       ** attributes has 3 arrays of data for runes, charms 4, consumable (choocho is 2)
       ** media status and updatedat the date it was minted/bought??, doesn't seem to change if overridden with a new purchase of same type...
       ** opensea as listed marketplace??, floor_price_usd and price_usd original price per item, price and price_currency matches whatever the currency is


       metadata, paired with?
       to make??
       scour lb to make a metric???
       what metric exactly...????
    }

handling sone stuff...
